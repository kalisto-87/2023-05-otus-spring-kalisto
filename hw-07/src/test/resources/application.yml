spring:
  shell:
    interactive:
      enabled: false
  datasource:
    url: jdbc:h2:mem:testdb
    username: phantom
    password:
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true