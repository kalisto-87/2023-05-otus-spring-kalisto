spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: phantom
    password:
  jpa:
    properties:
      hibernate:
        format_sql: false
    show-sql: true
    hibernate:
      ddl-auto: validate